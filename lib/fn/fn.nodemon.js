// Handle Auto Restart Restart
process.once("SIGUSR2", () => {
  console.log("-".repeat(50));
});

// Handle Auto Restart Restart
process.once("DEVMOD", () => {
  console.log("HEREEEEE".repeat(50));
});
