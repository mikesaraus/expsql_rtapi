{
  "name": "payment_system",
  "version": "0.0.2",
  "description": "Realtime API for Payment System",
  "private": true,
  "main": "app.js",
  "scripts": {
    "setup": "node app.js --gensql database.sql --genenv",
    "dev": "DEBUG=express:* nodemon -V --delay 2500ms --watch '*.*' --watch .env --ext 'js,json' app.js",
    "dev-log": "DEBUG=express:* nodemon -V --delay 2500ms --watch '*.*' --watch .env --ext 'js,json' app.js --log",
    "start": "DEBUG=express:* NODE_ENV=production node app.js --log 2>&1 | tee ./log/console-\"$(date '+%Y%m%d-%H%M%S')\".log -a -p"
  },
  "keywords": [
    "NodeJs",
    "Expressjs",
    "PostgreSQL",
    "Node-Cache"
  ],
  "author": {
    "name": "mikesaraus"
  },
  "license": "0BSD",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "compression": "^1.7.4",
    "dotenv": "^10.0.0",
    "drivelist": "^9.2.4",
    "express": "^4.17.2",
    "formidable": "^2.0.1",
    "helmet": "^5.0.1",
    "jsonwebtoken": "^8.5.1",
    "lodash.get": "^4.4.2",
    "morgan": "^1.10.0",
    "os": "^0.1.2",
    "pdfkit": "^0.13.0",
    "pg": "^8.7.1",
    "rotating-file-stream": "^3.0.2",
    "socket.io": "^4.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
